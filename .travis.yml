language: node_js
node_js:
  - "11.10.1"
cache:
  directories:
  - node_modules
script:
  - npm install
  - npm test
after_success: 
  npm run coverage
after_script: # Upload coverage reports 
  - COVERALLS_REPO_TOKEN=$coveralls_repo_token npm run coveralls